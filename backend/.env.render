# EnglishMaster Pro Backend - Environment Variables for Render

# Server Configuration
PORT=10000
NODE_ENV=production

# Database Configuration (TiDB Cloud)
# You'll need to set these in Render's environment variables dashboard
DB_HOST=gateway01.ap-southeast-1.prod.aws.tidbcloud.com
DB_PORT=4000
DB_USER=your-tidb-username
DB_PASSWORD=your-tidb-password
DB_NAME=your-tidb-database

# JWT Secret (Generate a strong secret for production)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Frontend URLs (Update after frontend deployment)
FRONTEND_URL=https://eduplatfrom.vercel.app

# Optional: Additional security settings
BCRYPT_ROUNDS=12
